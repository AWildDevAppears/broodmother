import Game from '../../store/Game';

it( 'should allow a default brood to be added to the pack', () => {
    // expect( Game.currentBrood.length ).toBe( 0 );
    // expect( Game.food ).toBe( 0 );
    
    // Game.food = 10;
    // Game.addNewBrood();

    // expect( Game.currentBrood.length ).toBe( 1 );
    // expect( Game.food ).toBe( 0 )
} )